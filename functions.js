module.exports = {

  returnTwo: () => {
    return 2;
  },

  greeting: (name) => {
    return `Hello, ${name}.`;
  },

  add: (x, y) => {
    return x + y;
  },

  multiply: (x, y) => {
    return x * y;
  },

  divide: (x, y) => {
    return x / y;
  },

  subtract: (x, y) => {
    return x - y;
  }
}